{
  "private": true,
  "name": "creative-cloud-libraries",
  "version": "0.0.7",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/AdobeDocs/creative-cloud-libraries"
  },
  "dependencies": {
    "@adobe/gatsby-theme-aio": "^3.0.7",
    "gatsby": "^3.0.0"
  },
  "scripts": {
    "start": "gatsby build && gatsby serve",
    "start:prefix": "gatsby build --prefix-paths && gatsby serve --prefix-paths",
    "dev": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  }
}
